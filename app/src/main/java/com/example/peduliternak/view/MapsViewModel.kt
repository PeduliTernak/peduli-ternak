package com.example.peduliternak.view

import androidx.lifecycle.LiveData
import androidx.lifecycle.ViewModel
import com.example.peduliternak.data.UserRepository

class MapsViewModel(private val repository: UserRepository) : ViewModel() {
//    fun getStoriesWithLocation(location: Int): LiveData<Result<ResponseGetStory>> {
//        return repository.getStoryLocation(location)
//    }
}